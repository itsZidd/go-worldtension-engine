# WorldTension Data Engine

An automated ETL (Extract, Transform, Load) pipeline designed for geopolitical monitoring and data analysis. This engine ingests global news event streams, processes them using generative AI to derive quantitative stability metrics, and synchronizes the results with a centralized database.

---

## ⚙️ System Architecture

The engine operates as a scheduled serverless pipeline, executing the following lifecycle:

1. **Data Ingestion:** Downloads the latest 15-minute global event export from the **GDELT 2.0 Project** stream directly into memory.
2. **Code Mapping:** Translates legacy FIPS 10-4 country codes (used by GDELT) into ISO 3166-1 alpha-3 standards to ensure compatibility with modern mapping libraries and relational databases.
3. **AI Moderation:** Batches raw event data into chunks of 50 countries and utilizes **Gemini 3 Flash** to perform geopolitical sentiment analysis. The AI acts as a reasoning layer to transform qualitative headlines into structured data.
4. **Database Synchronization:** Executes a strict `UPSERT` operation on a **Supabase (PostgreSQL)** instance, updating country records with fresh metrics. Stale records (no update in 48h) are automatically decayed toward neutral scores.
5. **Frontend Integration:** Sends a POST request to a **Vercel Deploy Hook**, triggering a static rebuild of the frontend intelligence library to reflect the new processed data.

---

## 📊 Data Methodology

The engine transforms qualitative news data into four primary geopolitical metrics:

- **Tension Score (0–10):** Measures the immediate volatility of a region. High values indicate escalating conflict or diplomatic breakdown.
- **Stability (0–100%):** Assesses internal state health. High scores represent social cohesion and political order; low scores suggest civil unrest, protests, or institutional transition.
- **Industrial Capacity (0–100):** A composite metric reflecting economic output, infrastructure resilience, and resource mobilization potential.
- **Intelligence Briefing:** A natural language summary generated by the AI, synthesizing the day's most impactful events into a concise tactical report.

---

## 🛠️ Tech Stack

| Category | Technology |
| :--- | :--- |
| **Language** | Go (Golang) 1.22+ |
| **Generative AI** | Google Gemini 3 Flash |
| **Database** | Supabase (PostgreSQL) |
| **Orchestration** | GitHub Actions |
| **Data Source** | GDELT 2.0 Event Stream |
| **Integration** | Vercel Deploy Hooks |

---

## 🚀 Getting Started

### Prerequisites

- Go 1.22 or higher installed locally.
- A Supabase project with a configured `world_tension` table.
- A Google AI Studio API Key.

### Local Environment Setup

Export the following environment variables in your terminal before running the engine:

```bash
# Database Connection
export DATABASE_URL="postgres://postgres.[ID]:[PASS]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# Google AI Studio API Key
export GEMINI_API_KEY="your-api-key-here"

# Vercel Rebuild Hook (Optional)
export VERCEL_DEPLOY_HOOK="your-webhook-url"
```

### Installation & Execution

1. **Clone the repository and install dependencies:**
   ```bash
   go mod tidy
   ```

2. **Run the pipeline manually:**
   ```bash
   go run .
   ```

---

## 📂 Project Structure

```text
.
├── main.go               # Orchestrator: config, clients, pipeline steps
├── gdelt.go              # GDELT ingestion: HTTP fetch, ZIP parsing, CSV mapping
├── ai.go                 # Gemini analysis: chunking, prompting, response parsing
├── db.go                 # Database: upsert, score clamping, decay logic
├── models.go             # Shared structs: CountryUpdate, AIResponse
├── countries.go          # FIPS → ISO dictionary + reverse map (174 sovereign states)
├── changelog.md          # Project versioning and logic evolution history
└── .github/
    └── workflows/
        └── daily-turn.yml  # GitHub Actions cron schedule configuration
```

---

## 🗄️ Database Schema

The engine targets a PostgreSQL table named `world_tension`:

| Column | Type | Description |
| :--- | :--- | :--- |
| `iso_code` | TEXT (PK) | ISO 3166-1 alpha-3 code (e.g., `USA`, `IDN`) |
| `fips_code` | TEXT | Legacy FIPS 10-4 code (e.g., `US`, `ID`) |
| `tension_score` | NUMERIC | AI-calculated tension level (0.0–10.0) |
| `stability_score` | INTEGER | Domestic stability percentage (0–100) |
| `industrial_capacity` | INTEGER | Economic mobilization metric (0–100) |
| `recent_activity` | TEXT | AI-generated intelligence briefing |
| `event_count` | INTEGER | Raw headlines processed this turn |
| `last_updated` | TIMESTAMP | Timestamp of the latest update |

### Required Constraint

```sql
ALTER TABLE world_tension
ADD CONSTRAINT tension_score_range
CHECK (tension_score BETWEEN 0.0 AND 10.0);
```

---

## 🔒 Data Integrity

- **Score Clamping:** All AI-generated scores are clamped to valid ranges in Go before any database write, preventing bad model outputs from corrupting the dataset.
- **Score Decay:** Countries absent from GDELT for more than 48 hours are automatically nudged back toward neutral each run (tension −0.5/day, stability +2/day, industrial +1/day).
- **Chunk Validation:** If Gemini returns fewer than half the expected updates for a batch, a warning is logged for review.
